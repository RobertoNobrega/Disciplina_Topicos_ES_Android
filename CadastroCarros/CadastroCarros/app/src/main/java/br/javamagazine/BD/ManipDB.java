package br.javamagazine.BD;

import android.content.Context;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteOpenHelper;

// Autor : Roberto

/*
public class ManipDB extends SQLiteOpenHelper {    // OBS : SQLiteOpenHelper é uma classe abstrata.

    private static ManipDB obj;


    private ManiDB(Context contexto){
        super(contexto,)
    }

    public static ManipDB inst(Context contexto){
        if(obj == null)  // Ocorre apenas a instanciação do objeto uma única vez.
            obj = new ManipDB(contexto);
        return ManipDB;
    }

    @Override
    public void onCreate(SQLiteDatabase db){ }

    @Override
    public void onUpgrade(SQLiteDatabase db, int oldVersion, int newVersion){ }
}
*/